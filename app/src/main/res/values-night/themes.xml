<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.ParkWhere" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/white</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/black</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_200</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">?attr/colorPrimaryVariant</item>
        <!-- Primary text color. -->
        <item name="android:textColorPrimary">@color/light_grey_E8E5E0</item>
        <!-- Default UI background color. -->
        <item name="android:windowBackground">@color/dark_08</item>

        <!-- Animation to eliminate flicker when switching themes -->
        <item name="android:windowAnimationStyle">@style/ThemeAnimationTransition</item>
        <item name="android:windowContentTransitions">true</item>
    </style>

    <!-- Splash screen theme. -->
    <style name="Theme.ParkWhere.SplashScreen" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/black</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/splash_screen</item>
        <item name="postSplashScreenTheme">@style/Theme.ParkWhere</item>
    </style>
</resources>